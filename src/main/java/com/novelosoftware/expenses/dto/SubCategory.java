package com.novelosoftware.expenses.dto;

/**
 * Granular expense subcategory (Categoria) that maps to a parent Category.
 */
public enum SubCategory {
    /** Personal savings. */
    SAVINGS,
    /** Miscellaneous or uncategorized expenses. */
    MISCELLANEOUS,
    /** Monthly rent. */
    RENT,
    /** Grocery / supermarket shopping. */
    GROCERIES,
    /** Credit card payment. */
    CREDIT_CARD_PAYMENT,
    /** Expenses related to spouse. */
    SPOUSE,
    /** Car accessories and parts. */
    CAR_ACCESSORIES,
    /** Vacation and travel expenses. */
    VACATION,
    /** Highway or bridge tolls. */
    TOLLS,
    /** Food purchases (non-restaurant). */
    FOOD,
    /** General utility bills. */
    UTILITIES,
    /** Bank or peer-to-peer transfer. */
    TRANSFER,
    /** Credit or debit card fees/charges. */
    CARD,
    /** Vehicle leasing payment. */
    LEASING,
    /** Gasoline / fuel. */
    GAS,
    /** Going out / social outings. */
    OUTING,
    /** Childcare / nanny expenses. */
    NANNY,
    /** House cleaning services. */
    CLEANING,
    /** Day-to-day running expenses. */
    CURRENT_EXPENSE,
    /** Truck-related expenses. */
    TRUCK,
    /** T-Mobile phone bill. */
    CELPHONE_BILL,
    /** Restaurant dining. */
    RESTAURANT,
    /** Electricity bill. */
    ELECTRICITY,
    /** General services. */
    SERVICES,
    /** Software purchases or licences. */
    SOFTWARE,
    /** Entertainment (movies, concerts, etc.). */
    ENTERTAINMENT,
    /** Monthly car instalment payment. */
    CAR_MONTHLY_PAYMENT,
    /** Streaming or software subscriptions. */
    SUBSCRIPTIONS,
    /** Personal loan repayment. */
    LOAN,
    /** Other uncategorized expenses. */
    OTHER,
    /** International wire transfer. */
    INTERNATIONAL_TRANSFER,
    /** Rideshare (Uber, Lyft, etc.). */
    RIDESHARE,
    /** Home-related expenses. */
    HOME,
    /** Debt repayment. */
    DEBT,
    /** Parking fees. */
    PARKING,
    /** Car wash. */
    CAR_WASH,
    /** Medical / health expenses. */
    HEALTH,
    /** General car expenses. */
    CAR,
    /** Lost or unaccounted money. */
    LOST,
    /** Settlement or liquidation payment. */
    SETTLEMENT,
    /** Internet service bill. */
    INTERNET,
    /** Tips left at restaurants or services. */
    TIP,
    /** Clothing and apparel. */
    CLOTHING,
    /** Electronic gadgets and accessories. */
    GADGETS,
    /** Income received. */
    INCOME
}
